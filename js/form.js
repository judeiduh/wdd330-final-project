// js/form.js
const form = document.getElementById('contact-form');
if(form){
  form.addEventListener('submit', (e)=>{
    // Let browser handle validation & submission (GET) to form-submission.html
    // Could add client-side enhancements here if desired
  });
}
